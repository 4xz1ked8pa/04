var React = require('react');

var MainHeader = React.createClass({
  render: function() {
    return (
      <header className="site-main-header">
        <h1 className="header-title">Charles Jackson</h1>
        
      </header>
    );
  }
});

module.exports = MainHeader;
